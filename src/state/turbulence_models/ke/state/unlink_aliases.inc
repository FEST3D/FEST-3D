!include in state file in subroutine name same as file name


            call dmsg(1, 'turbulence_models/ke/state', 'unlink_aliases.inc')
            nullify(tk)
            nullify(te)
            nullify(tk_inf)
            nullify(te_inf)
