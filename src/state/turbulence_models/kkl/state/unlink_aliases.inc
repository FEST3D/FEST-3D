!include in state file in subroutine name same as file name


            call dmsg(1, 'turbulence_models/kkl/state', 'unlink_aliases.inc')
            nullify(tk)
            nullify(tkl)
            nullify(tk_inf)
            nullify(tkl_inf)
