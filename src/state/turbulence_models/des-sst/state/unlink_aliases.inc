!include in state file in subroutine name same as file name


            call dmsg(1, 'turbulence_models/des-kw/state', 'unlink_aliases.inc')
            nullify(tk)
            nullify(tw)
            nullify(tk_inf)
            nullify(tw_inf)
